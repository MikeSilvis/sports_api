require "bundler/gem_tasks"
require 'sports_api'
require 'irb'
require 'irb/completion'

namespace :sports_api do
  desc 'Open a console with sports_api required'
  task :console do
    ARGV.clear
    IRB.start
  end
end
